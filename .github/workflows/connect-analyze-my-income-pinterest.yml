name: Disabled misplaced AMI OAuth workflow

on:
  workflow_dispatch:

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled. Analyze My Income OAuth and Pinterest automation belong only in pentacam-hub/calculate-engine."
