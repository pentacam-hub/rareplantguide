name: Disabled misplaced Analyze My Income workflow

on:
  workflow_dispatch:

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled. Analyze My Income Pinterest automation belongs only in pentacam-hub/calculate-engine."
