name: Disabled accidental cross-project workflow

on:
  workflow_dispatch:

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled: Analyze My Income and The Rare Plant Guide must remain separate Pinterest projects."
